# Base URL of the Cubestats backend API the browser should call.
# Unset = local dev default (http://localhost:8000).
# Must be reachable from the user's browser, so use the public backend URL.
NEXT_PUBLIC_API_BASE_URL=https://your-backend.example.com
