# Comma-separated list of origins allowed to call this API (CORS).
# Set this to your deployed frontend's URL(s). Unset = local dev default
# (http://localhost:3000).
CUBESTATS_ALLOWED_ORIGINS=https://your-frontend.example.com
